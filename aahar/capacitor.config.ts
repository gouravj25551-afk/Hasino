import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.aahar.feedfactory',
  appName: 'Aahar',
  webDir: 'dist',
  android: {
    backgroundColor: '#ffffff',
  },
}

export default config
